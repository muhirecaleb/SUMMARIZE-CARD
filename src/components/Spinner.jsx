import { ClipLoader } from "react-spinners";

const Spinner = () => {
  return (
    <ClipLoader
      color="#65b8fe"
      cssOverride={{}}
      size={50}
      speedMultiplier={1}
    />
  );
};

export default Spinner;
